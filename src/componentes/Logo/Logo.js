import React from 'react';
import Styles from '../Logo/Logo.module.css';

const Logo = (props) => {
  return (
    <div className={Styles.Container}>
      <p>MONICA GARCIA</p>
    </div>
  );
};

export default Logo;
